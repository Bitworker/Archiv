<h1>Kategorie Liste</h1>

<table>
  <tr>
    <th>Name</th>
  </tr>

<% @categories.each do |category| %>
  <tr>
    <td><%= category.name %></td>
    <td class="admin_table_action"><%= link_to 'Anzeigen', category %></td>
    <td class="admin_table_action"><%= link_to 'Bearbeiten', edit_category_path(category) %></td>
    <td class="admin_table_action"><%= link_to 'Löschen', category, confirm: 'Bist du sicher?', method: :delete %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'Neue Kategorie', new_category_path %>

