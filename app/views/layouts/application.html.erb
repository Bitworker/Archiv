<!DOCTYPE html>
<html>
<head>
  <title>Bitworker - <%= yield(:title) || "Kein titel" %></title>
  <%= stylesheet_link_tag    "application", :media => "all" %>
  <%= javascript_include_tag :defaults %>
  <%= javascript_include_tag "application" %>

  <%= csrf_meta_tags %>
</head>
<body>



<div id="user_nav">
  <%= link_to "Facebook Login", "auth/facebook" %>  <br>
  <br>
  <% if user_signed_in? %>
      Eingeloggt als <%= current_user.email %>. Das bist nicht du?
      <%= link_to "Logout", destroy_user_session_path, :method => :delete %>
  <% else %>
      <%= link_to "Registrieren", new_user_registration_path %> oder <%= link_to "Login", new_user_session_path %>
  <% end %>
</div>

<% flash.each do |name, msg| %>
    <%= content_tag :div, msg, :id => "flash_#{name}" %>
<% end %>

<%= yield %>

</body>
</html>
